{
  "devDependencies": {
    "autoprefixer": "^10.4.12",
    "postcss": "^8.4.16",
    "tailwindcss": "^3.1.8"
  },
  "scripts": {
    "watch":"npx tailwindcss -i tailwind.css -o ../final.css --watch",
    "build":"npx tailwindcss -i tailwind.css -o ../final.css --minify",
    "predeploy":"npm run watch",
    "deploy":"gh-pages -d build"
  }
}